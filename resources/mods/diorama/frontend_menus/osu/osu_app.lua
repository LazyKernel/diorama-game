

--------------------------------------------------
return function (menu)

	local instance = {
		parentMenu = menu,
		canvas = nil,
        map_name =
	}

	Mixin.CopyTo (instance, c)

	return instance
end
